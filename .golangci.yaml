run:
  timeout: 10m
linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
issues:
  max-issues-per-linter: 0 # Unlimited
  max-same-issues: 0 # Unlimited
